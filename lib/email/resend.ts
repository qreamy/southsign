import { Resend } from 'resend';
const appName=process.env.NEXT_PUBLIC_APP_NAME||'Southbase Sign';
const from=process.env.RESEND_FROM_EMAIL||'Southbase Sign <onboarding@resend.dev>';
function client(){if(!process.env.RESEND_API_KEY)return null;return new Resend(process.env.RESEND_API_KEY)}
export async function sendSigningEmail(to:string,name:string,documentName:string,link:string){const r=client();if(!r){console.warn('RESEND_API_KEY missing; signing URL:',link);return}await r.emails.send({from,to,subject:`Dokument för signering: ${documentName}`,html:`<div style="font-family:Arial,sans-serif;max-width:600px;margin:auto;padding:30px"><h2>Du har fått ett dokument för elektronisk signering</h2><p>Hej ${escapeHtml(name)},</p><p><b>${escapeHtml(documentName)}</b> väntar på din granskning och signering.</p><p><a href="${link}" style="display:inline-block;background:#2457e6;color:#fff;padding:13px 18px;border-radius:9px;text-decoration:none;font-weight:700">Granska och signera</a></p><p style="color:#667085;font-size:13px">Länken är personlig. Vidarebefordra den inte.</p><p>${appName}</p></div>`})}
export async function sendSignedConfirmation(to:string,documentName:string,verifyUrl:string,pdf?:Buffer){const r=client();if(!r)return;await r.emails.send({from,to,subject:`Signering klar: ${documentName}`,html:`<div style="font-family:Arial,sans-serif;max-width:600px;margin:auto;padding:30px"><h2>Dokumentet är signerat</h2><p>${escapeHtml(documentName)} har signerats elektroniskt.</p><p><a href="${verifyUrl}">Verifiera dokumentet</a></p></div>`,attachments:pdf?[{filename:`${documentName.replace(/[^a-zA-Z0-9_-]+/g,'_')}-signerad.pdf`,content:pdf}]:undefined})}
export async function sendAdminConfirmation(documentName:string,documentId:string){const to=process.env.ADMIN_NOTIFICATION_EMAIL;const r=client();if(!to||!r)return;await r.emails.send({from,to,subject:`Signerad: ${documentName}`,html:`<p>Dokumentet <b>${escapeHtml(documentName)}</b> har signerats.</p><p>Dokument-ID: ${documentId}</p>`})}
function escapeHtml(s:string){return s.replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]!))}
