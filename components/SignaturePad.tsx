'use client';
import { useEffect,useRef } from 'react';
import SignaturePadLib from 'signature_pad';
export default function SignaturePad({onChange}:{onChange:(data:string)=>void}){const ref=useRef<HTMLCanvasElement>(null);const pad=useRef<SignaturePadLib|null>(null);useEffect(()=>{const c=ref.current!;const resize=()=>{const ratio=Math.max(window.devicePixelRatio||1,1);const rect=c.getBoundingClientRect();c.width=rect.width*ratio;c.height=180*ratio;c.getContext('2d')!.scale(ratio,ratio);pad.current?.clear()};pad.current=new SignaturePadLib(c,{minWidth:1,maxWidth:2.4});const done=()=>onChange(pad.current!.isEmpty()?'':pad.current!.toDataURL('image/png'));pad.current.addEventListener('endStroke',done);resize();window.addEventListener('resize',resize);return()=>window.removeEventListener('resize',resize)},[onChange]);return <div className="sign-area"><canvas ref={ref} className="canvas"/><div style={{padding:8,textAlign:'right'}}><button type="button" className="btn secondary" onClick={()=>{pad.current?.clear();onChange('')}}>Rensa</button></div></div>}
